{
  "name": "market-view",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node local-server.js",
    "market-view:llm-test": "node scripts/generate-market-view-report-openrouter.js",
    "start": "node local-server.js",
    "check": "node --check local-server.js && find src api scripts test public -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "npm run check && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pngjs": "^7.0.0"
  }
}
